import 'package:flutter/material.dart';

class AppColor {
  static const pColor = Colors.deepPurple;
  static const sColor = Color(0xFFFA557D);

  static const dColor = Colors.white;
  static const deepGrey = Colors.grey;

  static const deepOrange = Colors.deepOrange;
  static const black = Colors.black;
}
